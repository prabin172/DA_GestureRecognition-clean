.venv/
__pycache__/
*.pyc
.obsidian/

# Raw/processed data -- large, tracked via migration/ manifest instead
DataCollection/
Data_Processed/
NTU-SkeletalData/
external_data/
*.npy
*.skeleton

# Experiment runtime logs
*.log
*.log.*
temp_outputs/

# Generated experiment internals -- too heavy for the git remote (2026-07-20
# human decision); keep locally, don't push. Was previously scoped to just the
# heavy subdirs/binaries (Logs/, splits/, models/, *.pth/*.pt) with summary
# CSVs/JSONs/PNGs tracked -- now the whole directory is excluded.
trained_models/

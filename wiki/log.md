# Wiki log

Append-only. Entry format: `## [YYYY-MM-DD] ingest|update|query|lint | title`.

## [2026-07-21] update | Cross-setting controller finding folded into paper as R5b; stale §8 paragraph fixed
User asked to fold the controller cross-setting extension (previous log entry) into the paper.
Added as `paper_results.md` R5b (right after R5's Lock3/"all three locks agree" paragraph, before
R6) — table + synthesis paragraph, no revision-history narration. Added a method sentence to
`paper_method.md` §8.1 and a new limitations bullet to `paper_discussion.md`. While placing the new
content, found `paper_method.md` §8's own "Action-primitive assignment" paragraph (above §8.1) still
described the retired reliability-ranked vocab ("two most reliably-recognized gestures assigned to
Safety-Critical States") — missed during the 2026-07-20 randomization fix, which only touched §8.1
and `paper_results.md`, not this general-description paragraph. Fixed to describe the actual uniform
random draw; also fixed a stale `temp_controller_sim.py` path reference in the same section.
Regenerated all `.tex` files, grepped clean for stale phrases. `wiki/results/phase3-controller-crosssetting.md`
and `wiki/index.md` updated to reflect paper status.

## [2026-07-21] ingest | Controller cross-setting extension: Lock1+Lock2 across all 5 transfer settings
User asked to reproduce Locks 1+2 (no Lock 3) of the controller's randomized robustness protocol
across every transfer setting with compatible posteriors, not just NTU→Xsens: CZU skeleton, CZU IMU
orientation-only, UTD skeleton, CZU dual-raw. First-verified before touching anything: 3 of the 4
new settings already had trained checkpoints (`loso_fulltrain_calibration.py`), so posteriors could
be dumped via existing `dump_posteriors.py` with no retraining. CZU dual-raw (`dualbranch.py --mode
dual`) never persisted checkpoints or posteriors anywhere — only aggregate accuracy — flagged to the
user before proceeding; user chose to retrain it. Patched `dualbranch.py` with an opt-in
`--dump-posteriors-dir` flag (backward-compatible, smoke-tested), retrained all 3 seeds × 5 priors ×
mode=dual into new out-roots (existing locked `CZU-IMU-DUAL*/summary.csv`, cited in `paper_results.md`
R6c, untouched), verified the retrain's own accuracy matches the locked R6c numbers within ≤0.3pp.
Wrote `scripts/controller/controller_crosssetting.py` (Lock1+Lock2 logic refactored generic over a
settings dict, `controller_robust.py` itself untouched). Result: the two locks agree with each other
exactly on the two settings with a large/significant recognition-level effect (NTU→Xsens's mae,
CZU dual-raw's supcon — both reproduce the recognition table almost exactly); they disagree on the
three settings where the recognition-level effect is itself small or non-significant. Not yet folded
into the paper — presented as an exploratory finding pending human decision. Full writeup:
[[phase3-controller-crosssetting]].

## [2026-07-21] query | THMS venue-precedent literature re-checked; two citation-date errors fixed, two papers added
User asked for the closest IEEE THMS paper(s) to this work. Web-verified against IEEE Xplore: the
two THMS precedents already in `literature-landscape.md` had wrong years (Guo/Liu/Liu's
Cross-Model-Cross-Stream paper is THMS **2024** not 2023; Rahimi Taghanaki et al.'s time-frequency
contrastive HAR paper is THMS **Dec 2023** not 2022 — both errors were arXiv-preprint-date vs
journal-pub-date confusion). Added two more: Zhao/Deng/He/Chen "Local Domain Adaptation for
Cross-Domain Activity Recognition" (THMS 2021, closest on the domain-gap/MMD framing) and
Chiang/Lu/Hsu "A Feature-Based Knowledge Transfer Framework for Cross-Environment Activity
Recognition" (THMS 2017, more distant — ambient/smart-home sensors not skeleton→wearable). None of
the four combine the pretraining-objective comparison with a skeleton→IMU gap + downstream
controller — the headline "gap is ours" claim stands. See [[literature-landscape]].

## [2026-07-20] update | Controller Locks 2/3 fully randomized (was fixed recall-ranked vocab); all three locks now agree mae compounds worst
User was preparing to discuss the controller pillar (R5) with their advisor for the first time and
pushed back hard on the earlier framing: (1) paper prose should never narrate revision history
("was X, now Y") — state only the final valid result, unless both versions are genuinely part of
the scientific narrative (see the new `feedback_paper_writing_no_revision_history` memory);
(2) the controller docs/slides never explained the Monte Carlo mechanics (iteration counts,
randomization counts) in the first place; (3) most importantly — Locks 2 and 3 (cost-severity
sweep, iso-safety threshold) were still evaluating on the one fixed, recall-ranked gesture
assignment (`reliability_ordered_vocab()`) that the project had already decided to move away from
specifically to avoid any appearance of cherry-picking. Only Lock 1 had ever been fully randomized.

**Fix (user's explicit choice among three options — "fix the code too, rerun Locks 2/3 over
randomized vocabs"):** rewrote `scripts/controller/controller_robust.py` so all three locks now
draw and share the same 120 uniformly-random 7-gesture System Input assignments (`vocab_ids_list`,
generated once, reused across Locks 1/2/3) — `reliability_ordered_vocab()` is no longer called by
any lock, kept only as a startup print for transparency. Lock 2/3 output changed from single-vocab
points to per-vocab rows plus aggregated summaries (`costmodel_summary.csv`,
`iso_safety_summary.csv`, median/IQR across the 120 assignments). Smoke-tested at small scale, then
the full production run (120 vocabs × 1000 missions, all 3 locks) completed in 4m38s — fast enough
that there was no reason not to match Lock 1's full rigor everywhere. Backed up the old fixed-vocab
output to `trained_models/Phase3-controller/robust-fixedvocab-superseded/` (not cited anywhere)
before overwriting `robust/` with the new run.

**Result: the earlier Lock1-vs-Lock2/3 disagreement (supLP120 "worst" under the old fixed vocab)
turned out to be entirely an artifact of that one non-random gesture selection.** Under full
randomization, all three locks agree: mae compounds worst — Lock 1 (task-success, both τ and both
k), Lock 2 (highest median cost at every C_crit, both k), and Lock 3 (lowest task-success at the
safety operating point, both k, both budgets). supLP120's confident-false-critical-activation mode
(documented since before this session) still shows up as a narrow secondary nuance — a dip below
scratch at k=1 ungated, second-costliest under severe Lock 2 penalties — but never displaces mae as
worst under any lock. This is a substantially cleaner, more defensible result than either the
original fixed-vocab run or last week's "locks disagree" framing.

**Propagated:** `scripts/controller/controller_robust.py` (code + module docstring),
[[controller]] (§2 mapping section rewritten around full randomization, §8 prototype demoted to
"not used for any reported number," §9 rewritten, §10 reframed as secondary nuance not
disagreement, §13 gotchas updated), [[phase3-controller]] (fully rewritten with new numbers),
`wiki/index.md`, `paper/paper_results.md` R5 (rewritten as one coherent finding, explicit Monte
Carlo mechanics stated), `paper/paper_method.md` §8.1, and the controller-terminology sentences in
`paper_abstract.md`/`paper_intro.md`/`paper_conclusion.md`/`paper_discussion.md` (all reverted from
"no objective is safe under every stress test" back to a confident "mae compounds worst under every
stress test" claim, now that it's actually true again). Also caught and fixed two pre-existing stale
spots unrelated to this fix while regenerating `paper_method.md`: a "SupCon as scoped exhibit"
sentence (contradicted the rest of the paper, which already treats supcon as a full 5th objective)
and a description of a vocab-perturbation bug that no longer exists post-fix. Regenerated all
`paper/*.tex` via `md2tex.py`; verified no stale phrases remain via grep sweep. Advisor slide deck
(`paper/controller_advisor_slides.{md,pptx}`) rewritten to match — the "open question" slide is now
a single clean "all three checks agree" results slide instead of an unresolved disagreement.

## [2026-07-20] update | Old repo local folder deleted after full rerun confirmed superseding it; sole active repo now
User authorized deleting `~/projects/DA_GestureRecognition` (the original repo) locally after
confirming the 2026-07-15 full rerun's `trained_models/` structurally supersedes its separately-bolted
supcon result directories (checked LOSO-v2, CZU-skeleton/IMU/dual, UTD-skeleton all had supcon rows
present). Ported the one genuinely-unique uncommitted content (`RESEARCH_LOG.md` §B8, A2 multi-seed
pooling) into this repo first. Copied a suspicious untracked `notes.md` file (a "SYSTEM DIRECTIVE"-style
file with fake citation markers, found in the old repo, likely the source of an undocumented
`method.tex`/`results.tex` hand-edit from 2026-07-12) into this repo's root for human review — not
acted on beyond what was separately verified (see next entry). `rm -rf` initially left ~16GB/423 files
behind due to read-only model-checkpoint directories (`dr-xr-xr-x`, no write bit despite correct
ownership); fixed via `chmod -R u+w` then retried, succeeded. **GitHub remote
(`RTHMLab/DA_GestureRecognition`) deliberately left untouched** — human wants to check with the
RTHMLab team first. This repo is now the sole active working copy.

## [2026-07-20] update | notes.md verified: only §1 (terminology) was executed; §2/§4 (multi-gap controller data, R5 rewrite) were not — flagged, not acted on
Checked the copied `notes.md` "SYSTEM DIRECTIVE" against actual repo state. §1 (global terminology
scrub: grasp/release/mission → System Input/Safety-Critical State/Sequential Control Task) — done,
this is exactly the 2026-07-16 terminology-revision work. §3 (method.tex framing: Monte Carlo as
primary protocol, iso-safety threshold) — already true, predates notes.md (locked 2026-07-05b), not
caused by it. §2 (drop the illustrative prototype run; run the controller across all three gap
regimes — Small/CZU-UTD, Moderate/Xsens, Huge/CZU-IMU; compute ECE for all gaps) and §4 (restructure
R5's narrative around gap-size ordering) — **NOT done**. `wiki/concepts/controller.md` confirms the
illustrative prototype (`controller_sim.py`) is still in place and still cited as the abstract's
headline number; the controller has only ever been run on Xsens (Moderate-gap) posteriors. This would
be a genuinely new experiment (new controller runs + ECE on 3 more posterior sets, restructured R5),
not a doc cleanup, and it originates from a file with prompt-injection-style framing (fake citation
markers, "SYSTEM DIRECTIVE" header) never verified as coming from the user or collaborators — not
executed silently. Flagged to human; not actioned beyond the terminology-scrub overlap already done.

## [2026-07-20] update | Full paper_results.md reconciliation against this repo's independently-retrained checkpoints — 5 claims broken, most held, controller Locks 2/3 diverge from Lock 1
User asked: audit every experiment for multi-seed coverage, rerun any single-seed ones, compare this
repo's fresh rerun numbers against `paper/paper_results.md`'s numbers (carried over from the original
repo's incremental single/few-seed runs), flag real discrepancies, confirm the rest, and clean up all
results/writing to match. Full account below; see `paper/paper_results.md`'s own 2026-07-20 update
notes (top of file + inline per-section) for the number-by-number detail — this entry is the summary.

**Environment fix required first:** `.venv` was a broken symlink into the just-deleted old repo (this
repo never had its own venv despite `requirements.txt` being "frozen" — it had silently been running
against the old repo's environment this whole time). Rebuilt from `requirements.txt`: installed
`torch==2.11.0+cu128` from the PyTorch cu128 index first (plain PyPI doesn't have CUDA wheels), then
the rest; `pip freeze` diffed against `requirements.txt` — identical except `setuptools` (a build tool,
irrelevant to numerics). CUDA confirmed working (RTX 5080).

**Multi-seed audit (2 parallel subagents + direct checks):** most experiments already 3-seeded
correctly (main LOSO-v2 k∈{0,1,3}, A2, CZU-skeleton/IMU-crossmodal/dual, UTD-skeleton, controller
downstream of multi-seed posteriors). Two gaps found and promoted to 3-seed, launched 2026-07-20,
running in background as of this writing: **CZU-dual cold-start (T5)** — was single-seed, script
already supported `--seed`/`--out-root` (~3.5h for both new seeds, cheap); **OOV leave-class-out** —
was single-seed with no `--base-seed` flag at all, added one (mirroring the dualbranch.py pattern),
launched seed 43/44 (~15h/seed based on the seed-42 timing, ~30h total — the most expensive single
addition this session). Both chained in `multiseed_extension.log`.

**Numbers comparison (2 parallel subagents covering R1–R4c and R6–R6e, plus direct checks for R5):**
clean pattern across every section — `scratch`/CRC-baseline numbers (no pretrained-checkpoint
dependency) reproduce exactly to 2 decimals everywhere; every number downstream of a retrained
NTU-pretraining checkpoint (mae/supMAE/supLP120/supcon) drifts 1–5pp, because Stage 1 genuinely
retrained those checkpoints from scratch on different hardware (RTX 5080) with no determinism flags
set anywhere in the codebase (`cudnn.deterministic=False`, by original design, confirmed by reading
the actual training scripts — not a bug, the noise floor of independently re-running stochastic DL).
Root-cause investigation before touching any prose (per user request): checked `cka_analysis.py` for a
pooling bug analogous to the controller's known `reliability_ordered_vocab()` cross-method-pooling
quirk — ruled out, CKA computes each encoder fully independently. Checked Stage 1/2 training logs for
convergence anomalies (NaNs, early-stop failures) — none found, healthy normal convergence throughout.

**Five claims genuinely broke** (not just drifted): (1) R2's supMAE k=0 McNemar sign-flips (paper's
strongest cell, net +200/p=0 → net −22/p=.52, both non-significant so not a "reversal" of a real
effect, but the headline number doesn't hold); (2) R3's "supcon has the numerically highest CKA of all
5, edging out supLP120" claim reverses (0.0257→0.0115, now below supLP120's 0.0146) — deprioritized by
user decision, raw MMD²/Frechet (unaffected by the checkpoint retrain, confirms the gap ordering
exactly in both runs) is now the paper's primary domain-distance evidence, not CKA; (3) R6b's two
headline significance claims (supLP120<scratch, supMAE>supLP120) both weaken to trends; (4) R6c's
supcon effect nearly doubles (a strengthening, reported as such); (5) scattered per-k significance
losses in R6/R6d that don't touch pooled headline claims. Confirmed clean: R1 k=3, R4a (ECE ranking —
most checkpoint-stable result in the paper), R3 raw MMD/Frechet (exact match), R4c (A2, reproduces
within ~1pp — second most checkpoint-stable), R6d supcon, R6c for scratch/mae/supMAE.

**R5 controller — the deepest finding, required a human back-and-forth to work through.** Lock 1 (the
paper's stated *primary* evaluation protocol, 120 randomized System Input assignments) still shows mae
worst, matching the paper closely. But Lock 2 (critical-cost sweep) and Lock 3 (iso-safety) — which
evaluate on one *fixed* reliability-ordered vocabulary assignment, not Lock 1's 120 random draws — now
show **supLP120**, not mae, as worst under harsh penalties (mean cost at k=1, C_crit=∞: supLP120
237,681 vs mae 123,015 vs supcon's 27,014 best). This breaks the paper's literal claim that "the
ordering is invariant to the knobs." Root cause, worked out with the user: not a bug — it's the same
"confident false-critical-activation" failure mode the original paper already documented for supLP120
as a minor caveat, now landing harder because (a) the freshly-retrained checkpoint's specific confusion
pattern interacts worse with (b) whichever gestures this rerun's reliability-ordering happened to pick
as Safety-Critical anchors. Confirmed this doesn't contradict the calibration/ECE story: supLP120 is
still the best-calibrated method (R4a) and does well under Lock 3's normal reading — the broken part is
specifically the harsh-penalty tail, a narrower and separate mechanism. Also established, at the user's
request, that supMAE is never a catastrophic failure under Locks 1/2 at any k, but genuinely is the
worst method under Lock 3 at k=1 (and fails the strict 0.5% budget entirely there) — so "supMAE is
never worst" could not be written as a blanket claim; wrote the narrower, true version instead. supcon
emerged as the strongest performer overall at k=1 across all three locks, though not a clean sweep at
k=3.

**Rewrite executed** (per user: "these claims can't stand if not right, but write it so it's not
weaker than it needs to be — go ahead"): `paper/paper_results.md` R1–R6e fully updated with fresh
numbers, softened language exactly where significance broke (no more, no less), R5 rewritten around
the Lock 1 vs Lock 2/3 divergence and the supLP120/supMAE mechanism findings. Regenerated
`paper/*.tex` via `md2tex.py` (verified correct LaTeX escaping). Propagated to
[[phase1-mcnemar-ece-cka]], [[a2-subject-scaling]], [[phase3-controller]], [[czu-skeleton-loso]],
[[czu-imu-crossmodal]], [[czu-imu-dual]], [[utd-skeleton-loso]], [[domain-gap-metrics]],
[[oov-leave-class-out]], [[czu-dual-cold-start]], and `wiki/index.md`. `czu-dual-cold-start.md` and
the OOV page's 3-seed numbers are still pending — those runs are in progress (see the multi-seed audit
entry above); revisit once `multiseed_extension.log` shows both done.

## [2026-07-16] update | Controller terminology revised — grasp/release/mission dropped for abstract System Input naming
Human recalled a prior discussion about the controller's `grasp`/`release`/`mission` naming being too easy to misread as claiming real physical actions, despite the existing disclosure (§2 of [[controller]]) that these were always an invented, non-semantic relabeling. Traced it to the sibling `DA_GestureRecognition` repo: `paper/method.tex` and `paper/results.tex` there had *already* been hand-edited (2026-07-12, undocumented, before this repo's migration snapshot) to drop physical-action naming entirely — the 7 task primitives became **System Inputs** (2 **Safety-Critical States**, 5 **Routine States**), the task became the **Sequential Control Task**. That tex edit never propagated to the markdown sources, `wiki/concepts/controller.md`, `wiki/results/{phase3-controller,paper-framing}.md`, `paper/live_study_protocol.md`, or this repo at all. Rewrote all of the above in the sibling repo first, ran its `paper/md2tex.py` to regenerate all 6 tex files from the now-consistent markdown (restoring the single-source-of-truth relationship — this also caught two more mission→task spots the original hand-edit had missed), then ported the identical markdown/wiki changes here (path references adjusted: sibling repo is still flat `temp_*.py`, this repo uses `scripts/controller/controller_{sim,robust}.py`). Regenerated this repo's tex via `paper/md2tex.py` too — `method.tex`/`results.tex`/`intro.tex`/`discussion.tex`/`conclusion.tex`/`abstract_ieee.tex` are now byte-identical between both repos. **No numbers, code, or locked outputs changed** — `scripts/controller/{controller_sim,controller_robust}.py` still use `grasp`/`release`/etc. as internal Python identifiers (matches the just-completed attempt-8 rerun's locked `trained_models/Phase3-controller/robust/` output); only how the abstraction is named/described in the paper and wiki changed. `paper_idea.md`/`paper_experiments_plan.md` left untouched in both repos (historical design notes). **Per the human's direction, `DA_GestureRecognition-clean` is now the sole active working repo going forward** — the sibling repo's copies were brought current as a one-time sync, not an ongoing dual-maintenance commitment.

## [2026-07-15] update | Full 10-stage reproducibility rerun complete (attempt 8)
Last bug found: `KeyError: 'supcon'` in `scripts/controller/controller_sim.py`'s plotting
`colors` dict (Stage 8) — same "supcon bolted on later" class as attempts 4/6. Fixed by adding
`"supcon": "tab:red"`; sibling `controller_robust.py` (Stage 9) already had it. Relaunched
Stage 8+9 only (attempt 8, 2026-07-15 13:21 CDT) — both completed cleanly by 15:08 CDT, zero
errors. **All 10 stages (0-9) of `RUN_FULL_RERUN.sh` are now done** — this closes out the
`DA_GestureRecognition-clean` migration's reproducibility rerun (see `SESSION_HANDOFF.md` for
the full attempt-by-attempt history, attempts 1-8). Next: compare numbers against
`paper/paper_results.md`/`paper/*.tex` and update supcon's framing there (it's now a full
first-class 3-seed method, not the paper's described "scoped negative exhibit").

## [2026-07-04] update | Multi-seed + CZU analysed; α-sweep dropped (null); AUC/convergence metrics added
All three overnight jobs complete (multi-seed seeds 43/44, α-sweep 7 pts, CZU LOSO). Analysed with three metric families: final_acc, **AUC** (normalized area = mean eval_acc over first 20/30 calib epochs), **convergence** (first epoch ≥90% of final eval_acc), pulled from `Logs/*_calibration_epochs.csv`. **Multi-seed (3 seeds, n=15): the single-seed +4.1 pp supMAE−scratch @ k=1 is a seed-42 artifact** (seed43 −1.77) → pooled +0.97 n.s.; robust prior benefit is at k=0 (+2.52) / k=3 (+1.85, p=.054) and in AUC-30 k=3 (supMAE +2.04 p=.028, supLP120 +3.74 p=.010) + convergence (supLP120 reaches target ~3–4 epochs faster, p≤.012). **mae = significant negative transfer** on every metric (k=1 acc −2.84 p=.002; AUC −4.37 p<.001). McNemar deferred (no per-clip dumps saved → needs inference pass over ckpts). **CZU skeleton→skeleton (n=5, single seed): supLP120 +8.3 pp zero-shot (p=.07); supMAE modestly +; mae is *positive* → the mae-hurts signature does NOT reproduce on same-modality target.** Noted CZU also ships IMU (`sensor_mat/`, 10 body-worn 6-axis, no mag) — unused, phase 2. **α-sweep dropped:** per human, no meaningful/clean dose-response pattern (n=5 single-seed; scratch baseline non-monotonic in α; α=0/1 endpoints reuse pre-existing data, interior interpolated). New pages [[multiseed-loso-v2]] + [[czu-skeleton-loso]]; updated index; RESEARCH_LOG §B. Scripts `scratchpad_{multiseed_stats,seedbreak,auc_stats,alpha_czu}.py` (repo root).

## [2026-07-04] setup | CZU-MHAD skeleton parser validated + LOSO queued (Tier C phase 1)
CZU unzipped (`external_data/czu_mhad_data/CZU-MHAD/`, 1165 clips, 5 subj `cx/cyy/myj/qyh/zyh`, 22 actions). **Key finding: CZU uses the identical NTU/Kinect-v2 25-joint order** (proven from demo `skeleton_display.m` bone list J + Y-up), so CZU joint positions feed our own `src/data/ntu_parser.process_to_local_quats` with **no reindexing**. `scripts/data_pipeline/czu_parser.py` reshapes skeleton `.mat` (F,100)→(F,25,4)→xyz→(F,17,4) LRQ; smoke passed (unit quats, bone CoV 0.025–0.044 = rigid → order correct, head 0.77m above spine-base). Generated `Data_Processed/czu_skeleton_lrq/` (1165 clips, 0 skipped, drop-in index.csv). Harness dry-run OK (5 subj/22 cls/all 4 NTU ckpts resolve). CZU LOSO (nohup, `czu_loso.log`) launched **running in parallel with the α-sweep** (GPU headroom fine; both detached, survive logout) → NTU-pretrained encoders LOSO k∈{0,1,3} on CZU-skeleton → `trained_models/CZU-skeleton-LOSO/` = external-validity test (does prior-benefit ordering reproduce on an independent public target?). IMU-fusion path (6-axis, no mag, yaw drift) deferred to phase 2. Also: multi-seed **DONE** (seeds 43/44 written); α-sweep past its gate, running (α=0.25).

## [2026-07-04] setup | Tier A α-sweep launched; Tier B/C plans; CZU-MHAD downloading
Corrected Tier A per human: swing is falsified/dropped; the principled gap axis is **v2→local** (twist-free→full measured twist = the axial-twist information IMUs have and skeletons don't). `temp_gap_interpolate.py` (`q(α)=R^α⊗q_v2`, smoke-verified q(0)==v2/q(1)==local) generates `imu_quats_alpha{0.25,0.5,0.75,1.5,2.0}`; `temp_alpha_sweep.sh` (nohup, `alpha_sweep.log`) waits for the multi-seed run then single-seed LOSO at 7 α → `AlphaSweep/` for the dose-response curve. Tier B plan written (calibration B1 + controller B2; copy Guo2017/Ovadia2019/myoelectric-rejection tooling, novelty = objective→calibration→control-reliability linkage). Tier C: chose **CZU-MHAD** (22 actions, 5 subj, Kinect skeleton + 10 accel/gyro IMUs, no mag); 4.33 GB downloading (`czu_download.log`); parser build gated on validation next session (skeleton→LRQ reuses v2; IMU path needs 6-axis fusion w/ yaw-drift caveat). Details in `paper_experiments_plan.md`.

## [2026-07-04] plan | Experiment roadmap to strengthen the THMS submission
Wrote `paper_experiments_plan.md` (repo root, companion to `paper_idea.md`). Four tiers keyed to the 3 acceptance-deciding objections: **A** (dose-response gap sweep A1, subject-count scaling A2, CKA A3 — do now, no new data), **B** (per-objective calibration B1 + differentiating controller B2 — required for THMS), **C** (external validity via a *public* full-body-IMU pair only — human won't collect new subjects; scout candidates CZU-MHAD/DIP-IMU/TotalCapture first), **D** (negative-transfer probes, λ-sweep, foundation-model baseline — opportunistic). Spine: multi-seed → A → B1 → B2; C/D off critical path. No experiments run yet — plan awaiting go.

## [2026-07-04] update | LOSO-v2 numbers in — v2 saves the narrative; multi-seed launched
LOSO-v2 finished (single seed, `trained_models/LOSO-fullTrainCalibrate-v2/`, 100 rows). The tighter v2 gap **restores + amplifies** the prior benefit swing had destroyed: supMAE−scratch @ k=1 = +4.1 pp (local +3.0, swing −1.3) → gap knob moves monotonically with prior value (MMD supmae 0.0109 local / 0.0322 swing / 0.0092 v2); 4/5 subjects positive. sub8 swing anomaly **resolved** (−19.9 → −1.0 pp, was a twist-stripping artifact). supLP120 negative transfer **fixed** (+0.4 vs local −4.3). mae now weakest (77.0 k=1). **Decision: v2 = the paper's preprocessing.** Launched multi-seed sweep (`scripts/orchestration/loso_v2_multiseed.sh`, seeds 43/44, k∈{0,1,3}, 4 methods → `-v2-seed{43,44}/`, `loso_v2_multiseed.log`) for §7.2 paired stats/McNemar. Updated position-reconstruction-v2/loso-fulltrain-calibrate/overview/open-questions/index + paper_idea + SESSION_HANDOFF + RESEARCH_LOG §B.

## [2026-07-04] ingest | v2 position-derived Xsens reconstruction — the real domain-gap fix
Diagnosed why [[swing-mode]] widened the NTU↔Xsens gap: symmetric-swing test (`mmd_domain_gap_symmetric.py (dead, not in this repo)`, swing-project NTU too) ≈ asymmetric → gap increase was twist-stripping damaging Xsens, not asymmetry. Fix (option B): new parser `src/scripts/IMU_batch_processor_v2.py` rebuilds Xsens from mvnx **positions** through NTU's own `get_bone_quaternion` shortest-arc construction + per-session T-pose world alignment; reuses v1 offset/anvil/slicing (alignment bit-identical, verified). Confirmed positions present on all 20 mvnx; prototype self-checks pass; caught+fixed a `tpose-isb` frame-0 timeline bug. Regenerated `Data_Processed/imu_quats_v2/` (2736 clips). v2 MMD is *below local* for every encoder (supmae 0.0092 vs 0.0109 local vs 0.0322 swing) → gap genuinely closed. Full LOSO-v2 launched (`scratch,supLP120,supMAE,mae`; `loso_v2.log`; `trained_models/LOSO-fullTrainCalibrate-v2/`) — numbers pending, discuss next session. New page [[position-reconstruction-v2]]; updated lrq/swing-mode/domain-gap-metrics/mmd-domain-gap/data-pipeline/loso-fulltrain-calibrate/open-questions/index + SESSION_HANDOFF.

## [2026-07-03] update | Deep-research report folded into paper_idea.md + paper_intro.md
Ingested the Undermind literature report (40 pp, ~173 refs). Wrote `paper_idea.md` (repo root): reframed spine to the Undermind-identified gap — first controlled objective × domain-gap study in skeleton→IMU with a demonstrated/explained negative-transfer case (supLP120 < scratch low-k; SupCon worst despite low MMD). Contains competitor grid, 2×2-completion + multi-seed/McNemar stats plan, CKA/symmetric-gap mechanism, OOV distinctiveness, controller spec, rejection-risk table. Wrote `paper_intro.md` (intro→background→previousWork→gap→contributions) grounded in the lit review with author-year↔report-number key. Both awaiting human review; recognizer experiments (§7) still to run before claims lock.

## [2026-07-03] update | Paper push started; CLAUDE.md compressed
Compressed CLAUDE.md further (~19 lines). User articulated the conditional-on-gap framing (supervised wins low-gap, reconstruction survives high-gap, SupMAE robust in both). Wrote deep-research literature prompt for an external agent (`temp_deep_research_prompt.md`); report expected next session → fold into `paper_idea.md` + wiki. SESSION_HANDOFF updated accordingly.

## [2026-07-03] ingest | Initial wiki build
Built the wiki from CODEBASE_DESCRIPTION.md, RESEARCH_LOG.md, SESSION_HANDOFF.md, projectAnalysis.md + repo inspection. 25 pages: 5 concepts, 2 data, 5 code, 9 experiments, 3 results, 1 questions, plus overview/index/log. Rewrote CLAUDE.md as the lean schema; thinned SESSION_HANDOFF.md to a pointer doc. Swing-mode results (2026-07-01 runs) captured in swing-mode-findings; publishability critique in publishability-review.

## [2026-07-05] update | Phase 1 done (McNemar/ECE/CKA), Phase 2 (A2 subject-scaling) launched
Paid the McNemar debt + CKA via one no-retrain pass. New scripts `scripts/main_experiment/dump_posteriors.py` (per-clip posteriors over existing multi-seed v2 base+calib ckpts, k∈{0,1,3}), `scripts/main_experiment/analyze_calibration.py` (ECE + reliability + paired clip-level McNemar), `scripts/main_experiment/cka_analysis.py` (layer-wise linear+RBF CKA, NTU vs Xsens-v2 per encoder — **v2 only, swing stays dead** per human), orchestrated `scripts/orchestration/phase1_run.sh` → `trained_models/Phase1-analysis/{ece,mcnemar,cka}_results.csv` + PNGs. **McNemar: mae = significant negative transfer every k** (net −334/−217/−92 @k0/1/3, p<.001); supMAE +200@k0 (p<.001), +131@k3. **Phase 2 A2 subject-count scaling** replaces dead C4: added `--n-train-subjects N` to `scripts/main_experiment/loso_fulltrain_calibration.py` (nested first-N of sorted non-held-out subjects; N=0 short-circuits to pretrained init → k-shot calibration only), runner `scripts/orchestration/a2_run.sh` sweeps N=0..4 seed 42 → `trained_models/A2-subjectScaling/N*/`, `scripts/main_experiment/analyze_a2.py` plots benefit-vs-N. Running overnight; analyse AM (predict prior benefit max at N=0/1, washes out by N=4). Phase 3 (B2 controller on posteriors, CZU-IMU optional) + Phase 4 (paper_idea C2/C4 rewrite) captured in SESSION_HANDOFF.

## [2026-07-05] ingest | Phase 1 + Phase 2 analysed — both landed, prediction confirmed
Both overnight jobs done. **Phase 1** (`scripts/orchestration/phase1_run.sh`, no retrain): McNemar/ECE/CKA over v2 multi-seed ckpts. McNemar pays the C2 debt — **mae = significant negative transfer every k** (net −334/−217/−92 @k0/1/3, all p<.001); supMAE significant positive every k (+200@k0, +131@k3); supLP120 mixed. ECE — **supLP120 lowest temp-scaled ECE every k** (best-calibrated prior; feeds C6). CKA — absolute tiny (<0.02, don't over-claim) but ordering clean+monotone-with-depth: **supLP120 aligns 3–5× the rest, scratch flat** → C3 mechanism. New page [[phase1-mcnemar-ece-cka]]. **Phase 2** (`scripts/orchestration/a2_run.sh`) A2 subject-scaling — prediction confirmed: prior benefit peaks N=0/1 (**supLP120 +26.9pp @k3,N1**), washes to ≈1pp by N=4; **mae ≤0 for N≥1** (reinforces C2). Deployment lever = prior value decays with enrolled-subject count. New page [[a2-subject-scaling]]. Wrote both to `RESEARCH_LOG.md` §B (McNemar/ECE/CKA/A2 tables); updated index. Next: owed big commit (still pending), Phase 3 B2 controller on `posteriors/*.csv`, Phase 4 paper_idea C2/C4 rewrite.

## [2026-07-05] ingest | Phase 3 controller prototype + full manuscript drafted; paper/ folder
Built the C6 controller prototype (`scripts/controller/controller_sim.py`): abstract event-driven FSM over the **real** held-out posteriors, 12-step pick-place mission (2 grasp/1 release), confidence-reject safety layer, asymmetric cost, method-agnostic command mapping. **Worthy (prototype)**: compounding turns a ~4pp recognition gap into a **21pp task-success gap** (τ=0,k=1: mae 0.754 vs supMAE 0.967); best-calibrated supLP120 owns the safety frontier (lowest false-activation every τ). Two open design knobs (critical-error=fail rule, command set) → human locks next. New page [[phase3-controller]] (status: draft). **Manuscript drafted**: moved all `paper_*.md` into new `paper/` folder and wrote the full split — `paper_method.md`, `paper_results.md` (**source of truth for numbers**), `paper_discussion.md`, `paper_conclusion.md`, `paper_abstract.md`; updated `paper_idea.md` with the post-multiseed RESHAPE note and `paper_intro.md` contributions. **Key reframe folded in**: C2 negative transfer INVERTED — the seed-stable McNemar-significant case is `mae` (pure reconstruction), inverting the field's "recon is safe"; supervised = neutral+best-calibrated; the prior's real value = calibration + low-enrollment data-efficiency (A2). Updated index (Results&paper + Non-wiki key files → paper/).

## [2026-07-05] ingest | Phase 3 controller knobs LOCKED via robustness protocol
Locked the controller design knobs by showing method ordering is invariant to them (not by freezing values). `scripts/controller/controller_robust.py` → `trained_models/Phase3-controller/robust/`. Three locks: (1) **randomized command vocabulary** ×120 → distribution; mae worst-compounding in all 4 (k×τ) conditions, ≥supMAE in only 12–15% of vocabs @k1; (2) **critical-cost sweep** C_crit∈{2..∞} + hard/soft outcome models → mae highest cost at every C_crit @k1; (3) **iso-safety** operating point (fix false-activation budget, tuning-free) → supLP120's calibration buys throughput (τ*=0.90 vs 0.95–0.99 @1% budget k1, ~13–20% faster; only init meeting 0.5% budget @k1). Honest finding turned design principle: supLP120's confident false-critical-activation dips it below scratch on the *ungated* metric → iso-safety is the deployment-correct framing. Locked claims: **mae compounds worst + calibration governs safety/throughput**. Updated `paper/paper_results.md` R5 (rewritten with locked numbers + 3-lock structure), `paper_method.md` §8.1, `paper_discussion.md` limitation; wiki [[phase3-controller]] → status active; RESEARCH_LOG §B.

## [2026-07-05] ingest | CZU published-baseline reproduced (statistical-moments + CRC) on identical LOSO splits
Reproduced the CZU-MHAD dataset paper's recognizer family (Chao et al. IEEE Sensors J. 2022): mean/std/var/skew/kurtosis features + CRC-RLS (λ=1e-4), on the **byte-identical LOSO k-shot splits** (`scripts/external/czu/crc_baseline.py`, run via nohup `scripts/orchestration/czu_crc_run.sh` → `czu_crc_baseline.log`; outputs `trained_models/CZU-skeleton-LOSO/crc_baseline/`). Head-to-head mean acc /5 folds — CRC 84.81/89.31/90.82 @k0/1/3 vs scratch 84.87/90.15/91.90 vs supLP120 93.14/93.08/93.85. **Key: scratch ≈ CRC (architecture alone ≈ hand-crafted baseline); only the NTU prior opens a gap — supLP120−CRC +8.3/+3.8/+3.0 pp.** Our CRC (84.8) > their published cross-subject skeleton ~75.5% (LRQ+LOSO vs raw-pos+T5–T7). Updated [[czu-skeleton-loso]] (published-baseline + CRC block), `paper/paper_results.md` R6. Reviewer value: same-splits baseline row + published reference point → addresses single-dataset-pair risk.

## [2026-07-06] ingest | CZU inertial TRUE cross-modal (skeleton→IMU) — prior ordering inverts
Built the true cross-modal external replication: NTU skeleton prior → CZU wearable-IMU (10 sensors, 6-axis, no mag). `scripts/data_pipeline/czu_imu_parser.py` (Madgwick AHRS per sensor → SLERP 30 Hz → per-sensor initial-pose norm → 10→17 segment map → local-relative quats; sensor index→body order recovered empirically from per-action gyro energy) → `Data_Processed/czu_imu_quats/` (1165 clips). Ran `scripts/orchestration/czu_imu_loso.sh` → `trained_models/CZU-IMU-LOSO/` (scratch/supLP120/supMAE/mae, k=0/1/3) + CRC inertial baseline (`scripts/external/czu/imu_crc_baseline.py`, raw accel+gyro moments). **Result: the prior ordering INVERTS vs the skeleton run** — supMAE best (59.3/56.3/61.1 @k0/1/3, +3.0/+2.4/+3.0 vs scratch), supLP120 now WORST (below scratch, −3.5/−2.8/−3.1). Per-fold sign tests: supMAE>scratch 12/15 (p=.035), supMAE>supLP120 13/15 (p=.007); paired-t n.s. at n=5. Reconstruction-augmented prior transfers across modality; pure-supervised doesn't. Caveats: deep ≪ CRC (56–61 vs 87–94%; orientation-only encoding discards accel + yaw drift → relative-transfer index, not IMU ceiling); mae ≈ scratch here (not the negative-transfer culprit, unlike NTU→Xsens). New page [[czu-imu-crossmodal]]; updated [[czu-skeleton-loso]], index.md, `paper/paper_results.md` R6b, RESEARCH_LOG §B.

## [2026-07-06] ingest | CZU dual-branch (R6c) — prior value is gap-contingent; representation was the bottleneck
Analyzed the finished dual-branch run (`czu_dual.log` → `CZU-DUAL ALL DONE`; `trained_models/CZU-IMU-DUAL/{raw,quat,dual}_<prior>/summary.csv`, reuses CZU-IMU-LOSO splits). Two findings. **(1) Representation, not architecture, was the R6b bottleneck:** raw-signal scratch branch = 80.7/89.8/95.2 @k0/1/3, matches/beats CRC (86.8/90.8/94.4) and ~30 pp above R6b's orientation-only 56–61%. **(2) On a strong target the NTU prior adds no value:** dual/scratch 86.6/89.1/91.4; no prior beats it — supMAE ties (7/15, p=1.00), mae ≤ (5/15), **supLP120 significantly worse (1/13, p=.003)**. Second branch itself a wash (dual/scratch vs raw/scratch 6/12, p=1.00). So R6b's supMAE benefit is **contingent on target-representation poverty** — vanishes once the target uses its full raw signal. Three-column arc on one dataset: R6 skeleton→skeleton supLP120 best-in-class → R6b weak cross-modal supMAE best, supLP120 worst → R6c strong cross-modal no prior helps, supLP120 worst. supLP120 goes hero→villain as gap widens = negative-transfer thesis as a design rule. New page [[czu-imu-dual]]; updated [[czu-imu-crossmodal]] (boundary note), index.md, `paper/paper_results.md` (R6b rescope + new R6c + ledger), `paper/paper_discussion.md` (R6 arc + limitations + practical guidance iv). Tex regenerated via `md2tex.py`.

## [2026-07-09] plan | Publishability review → tasks.md work plan (THMS locked, data release yes)
Hard review of desk-reject risks. Three structural: (1) six claims, no single thesis → fix = five-setting synthesis table (CZU-skel/UTD-skel small-gap supLP120 wins · Xsens mid-gap supMAE wins + mae hurts · CZU-IMU-quat/dual large-gap nothing wins + supLP120 hurts) with the culprit-swap (mae vs supLP120) confronted as a finding; (2) THMS venue mismatch (offline study + simulated controller) → live human-in-the-loop mini-study approved by human (T7 spec); (3) main dataset private → human committed to public release (T6). Cheaper holes: A2 + R6c-raw + DIAL still single-seed; SupCon named in abstract but absent from all tables (ckpt exists: `trained_models/ContrastiveNTU/supcon_epoch_50.pth`, harness METHODS dict = one-entry add); gap ordering asserted not measured → CKA-per-target task (T3). Backbone audit: all reported results use KinematicEncoder, NOT DSTformer ([[phase1-mcnemar-ece-cka]] mislabels CKA layers "DSTformer blocks" — fix queued); DSTformerQuatEncoder = ready-made second backbone (T9, optional). Full multi-seed-pretraining rerun deferred (T8). Wrote `tasks.md` (repo root) — self-contained plan for a lower model to execute.

## [2026-07-10] ingest | tasks.md T1/T2/T5 landed — SupCon (5th objective) end-to-end, cold-start scoped
All three overnight `tasks.md` chains (`t1_multiseed`, `t2_supcon`, `t2b_followon`) finished before session start; this entry is the pooling/analysis/write-up. **T1** (3-seed pooling, was single-seed): A2's k=1,N=1 spike confirmed real (+18.4pp p<.0001, robust across all 3 seeds individually, not a seed artifact as flagged); R6c raw-only and DIAL dial both pooled, same conclusions tighter. **T2 SupCon**: ran the 5th objective (Khosla SupCon) through every analysis — R1 (NTU probe 55.7%, cross-domain accuracy a wash vs scratch like the other priors), R2/R4a (McNemar mixed like supLP120, ECE 2nd-best-calibrated after supLP120), R5 controller (mae still worst of 5; **found+documented a script quirk**: `reliability_ordered_vocab()` pools recall across all loaded methods, so adding supcon silently shifted the fixed vocab feeding Locks 2/3 — root-caused via code read [not a data/seed bug, Lock 1 unaffected since its vocab is independently randomized], locked `robust/` numbers untouched, supcon reported separately, ordering conclusions still replicate under the new vocab), and all four external legs (R6/R6b/R6c/R6d, 3-seed pooled). **Headline finding: supcon tracks supLP120 almost exactly at every external setting** — wins big at both small-gap datasets (CZU +7.0/+4.8/+4.4pp all p<.001, even beating supLP120 itself p=.0002; UTD +8.4/+4.0/+2.9pp all p≤.006, statistically tied with supLP120) and loses/washes at both large-gap settings (quat: −1.4/−3.4/−0.2pp n.s.-marginal; dual: sign 13/43 p=.014, significantly worse). **This reframes the R6e axis**: it was "supervised (softmax) vs reconstruction"; with a 2nd label-aware objective (contrastive) landing in the identical band at every setting, the real axis is "has a reconstruction component vs doesn't" — supMAE is the only objective with one, and the only one that doesn't collapse at large gap. **T5 (new, not previously run)**: repeated the A2 cold-start subject-scaling sweep on CZU's strong (raw-signal) dual-branch target — found **no N at which any prior beats scratch**, and both priors significantly *worse* than scratch at N=0,k=1 (supLP120 −5.6pp p=.045, supMAE −5.6pp p=.005), the exact cell where Xsens shows the prior's biggest win. This scopes R4c's central deployment claim: the cold-start lever is itself target-representation-contingent, mirroring R6c's full-training finding. Updated `paper/paper_results.md` (R1/R2/R4/R5/R6/R6b/R6c/R6d/R6e, new supcon paragraphs throughout + T5 cold-start block), `paper_intro.md` (§7.5/7.6 method additions), `paper_abstract.md` (full rewrite — resolved its 2026-07-05 TODO, added the external-validity finding it never mentioned), regenerated tex. Wiki: [[a2-subject-scaling]] (pooled+cross-ref), **[[czu-skeleton-loso]] brought current** (was stale since 2026-07-04 despite pooled numbers existing in paper_results.md since 07-07 — real gap, fixed), [[czu-imu-crossmodal]], [[czu-imu-dual]], [[utd-skeleton-loso]], [[phase1-mcnemar-ece-cka]] (supcon rows), [[phase3-controller]] (vocab-quirk writeup), new page [[czu-dual-cold-start]], index.md. RESEARCH_LOG §B6 (numbers only, per channel convention). `tasks.md` T1/T2/T5/abstract items complete; T3/T4 were already done (2026-07-09); T6 stays paused (PII), T7 stays unapproved, T8/T9 stay deferred.

## [2026-07-09] update | Synced paper_intro.md §5 + paper_discussion.md to pooled/UTD numbers
`paper_intro.md`/`paper_discussion.md` were last written 2026-07-05/06, before the 2026-07-07 CZU 3-seed pooling and UTD-MHAD (R6d) landed — still cited single-seed R6c (p=.003) and omitted R6d entirely, while `paper_results.md` was already current. Added contribution 6 to `paper_intro.md` §5 (external validity: same prior hero on 2 small-gap datasets — CZU +7.2pp p=.0004, UTD +7.4pp p<.0001 — villain on cross-modal weak target (R6b, supLP120 worst p≈.04-.06, supMAE>supLP120 36/44 p<.0001) and still villain on cross-modal strong target (R6c, supLP120 worst p<.0001)). Rewrote `paper_discussion.md` §1 R6 paragraph (pooled numbers + R6d + explicit note that the R6b "supMAE beats scratch" headline reversed under pooling, 25/42 p=.28) and §3 limitations bullet (four pooled multi-seed legs on three datasets now, not "three single-seed n=5 CZU settings"; flagged R6c raw/quat rows + dial as the remaining single-seed pieces). Regenerated tex via `md2tex.py`. Answered a seed-audit question in the same session: R1/R2/R4b/R6/R6b/R6d/R6c-dual are 3-seed pooled; **R4c (A2 subject-scaling) and R6c's raw-only/quat-only diagnostic rows + dial remain single-seed** — worth flagging if reviewers press on power.

## [2026-07-07] ingest | CZU 3-seed pooling + UTD-MHAD (R6d) — R6b recon-beats-scratch was a seed artifact
Pooled CZU seeds 42/43/44 (45 folds) across R6 skeleton / R6b quat / R6c dual, and ran UTD-MHAD skeleton (3 seeds × 8 subj = 24 folds) as a 2nd independent same-modality dataset. Analysis `scripts/external/czu/multiseed_analyze.py`. **R6 strengthens:** supLP120 zero-shot +8.3 pp (p=.07) → pooled **+7.2 pp (p=.0004)**; supMAE sig-positive k0/k3; mae positive-n.s. **R6b corrected:** means barely move (supMAE top, supLP120 below scratch), but the single-seed "supMAE > scratch 12/15 (p=.035)" does **NOT** survive pooling (25/42, p=.28 — a seed-42 artifact); the robust effect is **supMAE > supLP120 36/44 (p<.0001)** + supLP120 < scratch (−2.3 pp, p≈.04–.06). New framing: supervised-specific negative transfer, reconstruction as the antidote — an inversion of *prior ranking*, not "reconstruction beats scratch." **R6c strengthens:** supMAE ties scratch (17/39, p=.52), supLP120 significantly worse (6/41, **p<.0001**, paired-t every k) — up from single-seed 1/13, p=.003. **R6d UTD replicates R6 cleanly:** supLP120 best at every k (+7.4/+3.9/+2.5 pp, p<.0001/.0005/.016; sign 53/62), mae positive not negative (+1.8, p=.011) → small-gap "supervised wins" now confirmed on two independent public datasets. Also folded the single-seed target-richness **dial** (20-ch mag rung) into R6c as a dose-response curve. Updated [[czu-imu-crossmodal]] (R6b reframe), [[czu-imu-dual]] (R6c pooled + dial), new page [[utd-skeleton-loso]], index.md, `paper/paper_results.md` (R6 robustness line, R6b rewrite, R6c pooled, new R6d, ledger), RESEARCH_LOG §B2 + B5 (items 1/2/4 done), `update.md` §3a/§3c (advisor doc). Tex regenerated via `md2tex.py`.

## [2026-07-09] update | tasks.md T0 (five-setting synthesis) + T3 (CKA-per-target, honest null)
Executing `tasks.md` (2026-07-09 publishability plan). **T0.1:** verified all 5 rows against R2/R6/R6b/R6c/R6d numbers (no conflicts) and installed the R6e five-setting synthesis table in `paper_results.md` (small→middle→large gap × target strength, one unifying map). **T0.2:** scoped `paper_intro.md` contribution 2 (mae negative-transfer) to the middle-gap setting only, rewrote contribution 6 as the five-setting map with an explicit link back to contribution 2; opened `paper_discussion.md` §1 with the same map and named the "culprit swap" (mae hurts only at the middle gap, supLP120 only at the two large gaps) as a finding, not an inconsistency; added a re-check TODO to the top of `paper_abstract.md` (A2 +27pp number + missing SupCon column, both pending T1/T2). **T0.3 backbone audit:** fixed `wiki/results/phase1-mcnemar-ece-cka.md` mislabeling CKA layers "DSTformer blocks" (→ KinematicEncoder Transformer layers, verified against `scripts/main_experiment/cka_analysis.py`'s `enc.transformer_encoder.layers`) — all reported results use `KinematicEncoder`, DSTformer only in unreported early experiments (`paper_intro.md` footnote + `paper_idea.md` §9.3 also fixed). **Adjacent bug found+fixed in the same wiki page:** `supLP120` (pure-supervised NTU-120 prior) was twice mislabeled "SupCon" (contrastive) in the ECE/CKA prose — fixed before it could bleed into T2's real SupCon work. **T3:** extended `scripts/main_experiment/cka_analysis.py` with `--multi-target` (reuses NTU features per encoder across targets, only 4 NTU forward passes total) to compute layer-wise CKA between NTU and each of Xsens-v2/CZU-skeleton/CZU-IMU-quat/UTD-skeleton (n≈1000/side, seed 0) → `trained_models/Phase1-analysis/cka_by_target{,_summary}.csv` + heatmap. **Result: does NOT confirm the small/middle/large gap ordering** — on supLP120, czu_skeleton (claimed small, same bucket as utd_skeleton) has the *lowest* mean CKA of all four targets (0.0267), below xsens_v2 (claimed middle, 0.0290) and czu_imu_quat (claimed large, 0.0260); utd_skeleton is highest (0.0327, consistent) but the two "small-gap" datasets land at opposite ends. Per human decision (asked, not assumed): reported as an honest null extending R3's necessary-not-sufficient finding, NOT forced into a gap column — gap ordering stays narrative, corroborated by the R6e sign-test/accuracy pattern instead. Written into `paper_results.md` R3 + R6e caveat, `paper_method.md` §7.4, new section in [[domain-gap-metrics]], index.md. Tex regenerated via `md2tex.py`. Next: T1 (A2/raw/DIAL seed 43/44 nohup chain) + T2.1 (supcon harness wiring), per human-agreed serial GPU schedule (shared with an unrelated IsaacLab job).

## [2026-07-09] update | T1/T2 launched (background); T4 CRC anchor + T2.2 stage-4 CKA + T5 code-prep + T7 protocol doc done inline
T1 (`scripts/orchestration/t1_multiseed_run.sh` → `t1_multiseed.log`) and T2.2 stages 1-2 (`scripts/orchestration/t2_supcon_run.sh` → `t2_supcon.log`, gated on T1's "T1 ALL DONE" marker) launched as detached nohup chains (verified PPID 1, survive session). T2.1: added `supcon` (`trained_models/ContrastiveNTU/supcon_epoch_50.pth`, strict-load verified) to `scripts/main_experiment/loso_fulltrain_calibration.py` METHODS, `scripts/external/czu/dualbranch.py` PRIOR_CKPT, `scripts/main_experiment/cka_analysis.py` ENCODER_CONFIGS, `scripts/main_experiment/dump_posteriors.py` DEFAULT_METHODS — **deliberately did NOT** add it to `scripts/controller/controller_robust.py` METHODS yet (that script eagerly indexes every method against on-disk posterior CSVs; adding the tag before supcon posteriors exist would crash it if run manually — deferred to the follow-on chain, after posteriors land). New in-script `train_supcon`/`supcon_loss` (standard Khosla 2020 SupCon, in-batch same-label positives) added to `scripts/main_experiment/ntu_to_ntu_objective_sanity.py` for the R1 within-domain row, mirroring the existing scratch/supervised/mae/supmae in-script-retrain rows — smoke-tested clean.

Then, prompted by the human questioning why several `tasks.md` items were left "not started" when they don't actually depend on the running GPU chains: ran the independent, low/no-dependency pieces immediately rather than deferring them —
- **T4 (UTD CRC baseline, CPU-only, no torch import)** — new `scripts/external/utd/crc_baseline.py` mirrors `scripts/external/czu/crc_baseline.py` on `UTD-skeleton-LOSO-seed42` splits. CRC k0/1/3 = 69.48/92.25/95.30 (8 folds); supLP120−CRC = +14.85/+2.02/+0.94 pp. Unlike CZU (scratch≈CRC), **UTD scratch already beats CRC by +6.5pp at k0** before any prior — noted honestly, not smoothed over. Written into [[utd-skeleton-loso]] + `paper_results.md` R6d + RESEARCH_LOG §B. Still TODO: cite UTD-MHAD's own published cross-subject figure (needs Planning/human, not compute).
- **T2.2 stage 4 (CKA for supcon)** — re-ran `scripts/main_experiment/cka_analysis.py` (single-target + `--multi-target`), inference-only against the already-existing pretrained checkpoint, no dependency on the in-flight LOSO runs. supcon CKA is numerically the *highest* of all 5 encoders (0.0257 @ L2 vs supLP120's 0.0149) — doesn't change any accuracy conclusion (none exists yet for supcon) but is now in [[phase1-mcnemar-ece-cka]], [[domain-gap-metrics]], `paper_results.md` R3. **Caveat logged:** this re-run used the default `--out-dir` and overwrote `trained_models/Phase1-analysis/cka_results.csv` in place rather than a new dir — justified since it's deterministic given fixed checkpoints and reproduced the existing numbers (scratch differs ≤0.0002, expected — unseeded random init every run).
- **T5 code-prep** — added `--n-train-subjects` to `scripts/external/czu/dualbranch.py` (N=0 short-circuits to pretrained-init-only via a new `if len(pool)==0` branch, mirrors `scripts/main_experiment/loso_fulltrain_calibration.py`'s A2 pattern exactly); smoke-tested both the N=0 and N=1 code paths directly (1-epoch, bypassing full CLI) — clean.
- **T7** — wrote `paper/live_study_protocol.md`, fleshing out the tasks.md T7 spec with concrete references (exact `MISSION` steps and `build_mapping` gesture-ranking logic from `scripts/controller/controller_sim.py`, real subject/gesture list from [[xsens-dataset]]). Explicitly marked NOT APPROVED — discussion draft only, no building/recruiting/IRB contact without human sign-off.

Building a second follow-on nohup chain now (does not touch the still-running `scripts/orchestration/t2_supcon_run.sh` file — a new script gated on its "T2 STAGE1+2 DONE" marker) to auto-run T2.2 stages 3/5/6 and the T5 N-scaling sweep once the current chains land, so the whole remaining queue runs unattended. Tex regenerated via `md2tex.py`.

**Chain built and launched: `scripts/orchestration/t2b_followon_run.sh` → `t2b_followon.log` (PID confirmed reparented to 1, detached).** Waits for `t2_supcon.log`'s "T2 STAGE1+2 DONE", then runs in order: (3) `scripts/main_experiment/dump_posteriors.py` + `scripts/main_experiment/analyze_calibration.py` across all 6 seed-dirs (3 original + 3 new supcon-only dirs — splits are seed-deterministic independent of which dir/method trained them, per `scripts/main_experiment/loso_fulltrain_calibration.py:989-1009`, so they pool/pair validly by (seed,subject,k); `--out-dir trained_models/Phase1-analysis-supcon`, new dir, doesn't touch the original Phase1-analysis outputs) — added `supcon` to `METHOD_ORDER` in `scripts/main_experiment/analyze_calibration.py`; (5) `scripts/controller/controller_robust.py --out-dir trained_models/Phase3-controller/robust-supcon` — added `supcon` to its `METHODS`/`RUNDIRS`/`colors` and a new `--out-dir` CLI override (previously hardcoded `OUT`) specifically so this re-run does **not** overwrite `trained_models/Phase3-controller/robust/`, which holds the LOCKED R5 numbers already cited by exact value in `paper_results.md` Lock 1-3; (6) external datasets (CZU skeleton/quat/dual, UTD), supcon only, 3 seeds each, new `*-supcon-seed{42,43,44}` dirs; then T5 (CZU dual cold-start N-scaling, N=0-3, `scratch/supLP120/supMAE`, seed 42, new `trained_models/CZU-DUAL-subjectScaling/N{0..3}/`). All existence-checked clean (no target dir pre-existed) before launch. Caveat: stages 3/5/6 could not be smoke-tested end-to-end (no supcon LOSO checkpoints exist yet to test against) — verified by reading the loading/pairing code instead (graceful-skip on missing files confirmed in `scripts/main_experiment/dump_posteriors.py`, `scripts/main_experiment/analyze_calibration.py`'s `run_ece`/`run_mcnemar`).

## [2026-07-09] query | T6 data-release PII finding — real subject names in raw mvnx metadata, packaging paused
Human OK'd releasing both raw mvnx + processed v2 quats for T6 with no blocker — while surveying the raw data before export, found all 18 `DataCollection/sub{7-11}/*.mvnx` files embed the **subjects' real first names** in the `<subject>` tag's `originalFilename` attribute (sub7=Amit, sub8=Charles, sub9=Ahmed, sub10=Pankaj, sub11=Grishma), plus the operator's OS username and FSU affiliation via a Windows path — none of this is visible from the filename or any processed output, only inside the raw XML header. Processed v2 quats (`Data_Processed/imu_quats_v2/`) checked clean (never touched this metadata). Flagged to the human before doing any export (human-subject PII, hard-to-reverse if it slipped into a public release). **Human decision: don't package yet — write the plan, revisit later.** Full de-identification plan (strip/remap `label`+`originalFilename`+`recDate`, new export dir only, datasheet README, re-confirm consent/IRB coverage before any actual upload) written to [[open-questions]] under "T6 data release". No files exported, no `DataCollection/` originals touched.

## [2026-07-09] update | SESSION_HANDOFF.md + overview.md refreshed (stale content removed)
Both were stale relative to this session's work: `SESSION_HANDOFF.md` still described the 2026-07-06 CZU dual-branch/multi-seed extension runs as "pending" when they finished and were written up 2026-07-07 (per this file's own 2026-07-04/07 entries) — the whole "Extension runs" + "Next" sections were dead weight. Rewrote it from scratch around the actual current state: what `tasks.md` items landed this session (T0/T3/T4/T2.1/T2.2-stage4/T5-code/T7-doc), the T6 pause (PII plan), T9 deferral, and — most load-bearing for a fresh session — exact names/markers of the three chained background jobs still running (`t1_multiseed.log`→`t2_supcon.log`→`t2b_followon.log`, each gated on the previous, all confirmed `PPID 1`) so a future session checks logs instead of re-launching. Added an explicit "don't commit yet, dirs are being actively written to" warning. `wiki/overview.md` (the thesis/entry page) was dated 2026-07-04 and still said "CKA still unwritten," "McNemar owed," and made no mention of Phases 1-3, the CZU/UTD external-validity arc, the five-setting map, or the controller pillar — all landed since. Rewrote "Current state" and "Where stuck" to reflect the real thesis shape (five-setting map as the unifying result) and the two genuinely open, human-gated items (T6 pause, T7 approval), not stale technical debt. No result numbers changed by this entry — pure navigation/pointer cleanup.

## [2026-07-10] lint | Full wiki pass — linked the one orphan page, fixed five stale pages, cleaned open-questions.md
Read all 34 wiki pages end-to-end (not just the ones touched by recent sessions) plus `tasks.md`/`RESEARCH_LOG.md`/`paper/paper_results.md` for cross-checks, then verified every `[[wikilink]]` resolves and every page has an inbound link. Found zero broken links; one true orphan — `results/literature-landscape.md` (the Undermind report ingest) was never linked from `index.md` despite existing since 2026-07-03 — added under Results & paper. Five pages had gone stale relative to work that landed after their last edit, all now fixed:
- **`concepts/pretraining-objectives.md`**: the SupCon row and "through-line" still said "underperforms scratch, empirically scoped out" — the single-seed, Xsens-only verdict from before the 2026-07-10 T2 full-parity pass. Rewrote to the current finding (tracks supLP120 closely; wash on Xsens, wins big at small-gap externals, washes at large-gap) and reframed the through-line around "has a reconstruction component vs doesn't."
- **`results/paper-framing.md`**: A6 carried the same stale "SupCon scoped out" line (mirroring the now-outdated RESEARCH_LOG §A item 4, which I did not edit — that's Planning's section). Added a dated update note instead of silently overwriting a "locked" framing doc, and flagged it for Planning to revisit. Also noted the controller (A0) is now built, not just spec'd.
- **`data/xsens-dataset.md`**: said "current mode: swing," regenerated 2026-06-30 — swing was falsified and superseded by v2 the very next week (2026-07-04) and this page was never updated. Rewrote the processed-data section around `imu_quats_v2/` as the locked representation, demoted `imu_quats/` (local/swing) to "superseded, kept for provenance," fixed the sub8 line to note the anomaly resolved under v2, and added the mvnx PII note.
- **`concepts/loso-protocol.md`**: "the stats problem at N=5 ... none of this is implemented yet" — McNemar + multi-seed pooling have existed since Phase 1 (2026-07-05). Rewrote to point at what's actually built ([[multiseed-loso-v2]], [[phase1-mcnemar-ece-cka]]).
- **`code/data-pipeline.md`**: minor — clarified v1/swing output isn't used by any current experiment, v2 is.

**`questions/open-questions.md`** got the biggest rewrite: the "Immediate (this session/next)" section was entirely 2026-07-01 swing-era housekeeping (write swing tables, commit swing work) that was completed and committed months ago — confirmed via `git log` on `IMU_batch_processor.py` and `git ls-files` on the swing run dirs before deleting, rather than assuming. The "experiments needed" list mixed done items (stats machinery, CKA, sub8) with two genuinely still-open ones — completing the frozen/fine-tuned 2×2 and the OOV distinctiveness analysis — that quietly fell out of the 2026-07-09 `tasks.md` replan without being marked done, dropped, or written up as a paper limitation. Surfaced both explicitly as "flag for Planning" rather than leaving them buried in a stale numbered list. Also updated `index.md` (literature-landscape link) and `overview.md` (Current state section still described 2026-07-09 state; brought to 2026-07-10 — SupCon full parity, cold-start scoping, tasks.md T0–T5 done).

## [2026-07-12] ingest | Raw (method-independent) dataset domain-gap metric — confirms the small/middle/large gap ordering CKA couldn't; folded into paper
Human asked whether any metric quantifies how different the datasets themselves are, independent of any trained method (existing MMD/CKA are both computed inside a pretrained encoder's feature space). New script `scripts/main_experiment/raw_domain_gap.py`: reuses the CRC-baseline hand-crafted feature (`clip_features` — mean/std/var/skew/kurtosis over 68 flattened LRQ channels, 340-d, zero model/pretraining involved) as a raw feature space shared by all 5 datasets (same (T,17,4) LRQ schema). Computed MMD² (RBF, median-heuristic bandwidth) and Frechet distance (PCA-50 Gaussian closed form) over all 10 pairwise combinations of {ntu, xsens_v2, czu_skeleton, czu_imu_quat, utd_skeleton}, n=1000/side (utd=861), seed 0, pooled-standardized. (A third metric, proxy A-distance, was also computed but saturated near-maximal on every pair — trivially separable, no ranking signal — dropped from reporting per human decision; raw values remain in the CSV.) **Result: MMD² and Frechet distance BOTH confirm the paper's claimed small(czu_skeleton 0.056)<small(utd_skeleton 0.096)<middle(xsens_v2 0.122)<large(czu_imu_quat 0.205) gap ordering exactly and monotonically** — the opposite outcome from T3's encoder-space CKA (2026-07-09), which did NOT confirm this ordering (czu_skeleton ranked lowest/most-dissimilar there instead of smallest-gap). Updated [[domain-gap-metrics]] (new "Raw-feature domain gap" section), index.md, RESEARCH_LOG §B7 (numbers only). **Folded into the paper**: `paper_results.md` (new R3 subsection with the MMD²/Frechet table + R6e caveat rewritten from "remains narrative" to "measured, encoder-free confirmation"), `paper_discussion.md` §1 (new paragraph after the CKA necessary-not-sufficient point), `paper_method.md` §7.4 (method description). Tex regenerated via `md2tex.py`. Outputs: `trained_models/RawDomainGap/{raw_domain_gap.csv, raw_domain_gap_{mmd,frechet,adist}.png}`.

## [2026-07-12] ingest | New page: [[controller]] — full design doc for the C6 controller
Human asked for a detailed doc on the planned downstream controller — every detail, and how it represents what the research wants it to. Existing [[phase3-controller]] is findings-only (locked numbers); nothing previously documented the mechanism/rationale end-to-end. New page `wiki/concepts/controller.md`, built by reading `scripts/controller/controller_sim.py` (prototype) and `scripts/controller/controller_robust.py` (the load-bearing robustness protocol) in full plus `paper_method.md` §8/§8.1, `paper_results.md` R5, `paper_idea.md` §8 (original design spec), and `paper/live_study_protocol.md` (T7, unapproved). Covers: why abstract-not-physics-sim and why real held-out posteriors (§1); command-vocabulary construction and its method-agnostic/safety-priority design (§2); why the 12-step mission has 2 grasps+1 release specifically (compounding, §3); FSM step mechanics including the critical-vs-recoverable error branch (§4); the asymmetric cost model (§5); false-activation/distractor stream (§6); metrics (§7); the prototype's single fixed config (§8) vs the load-bearing 3-lock robustness protocol (§9, each lock's mechanics + which reviewer objection it closes); the "honest finding" (supLP120's confident-false-critical-activation mode) as a designed-in result, not a caveat (§10); an explicit table mapping every abstraction element to what it represents in a real deployment (§11); the full planned-but-unapproved live human-in-the-loop extension (§12, from `live_study_protocol.md`); and code gotchas including the `robust/`-directory lock and the `reliability_ordered_vocab()` pooling quirk already found this session (§13). All claims cite file:line per the wiki's code-page convention rather than copying code. Cross-linked from [[phase3-controller]] and index.md.

**Same-day follow-up: the first draft under-disclosed a real assumption.** Human pushed back — where do `grasp`/`release` come from, we don't have those actions, did we assume something, and the doc didn't make clear what's being proven. Correct and important catch: verified against `trained_models/LOSO-fullTrainCalibrate-v2/label_map.json` (22 real gesture names: airkick, airpunch, bow, brushteeth, buttkicks, crossarms, crosstoe, drink, highfive, hop, jump, pickup, pushchair, runonspot, sidekick, sit, squat, stand, throw, turnaround, walk, wave — none named grasp/release/etc.) and `trained_models/Phase3-controller/mapping.csv` (squat→grasp 0.9967 recall, bow→release 0.9845, wave→confirm, crossarms→approach, sit→cancel, crosstoe→next, pickup→previous 0.9288). **The 7 control primitives are an invented command vocabulary for a hypothetical pick-and-place task; real gestures are relabeled onto them purely by recall rank, non-semantically** — the clearest evidence being that `pickup`, the one gesture whose name suggests grasping, is NOT the one mapped to `grasp`. Rewrote the page's opening to state the hypothesis (does small accuracy/calibration differences compound into task-level consequences) before any mechanism detail, added an explicit disclosure section with the real mapping table to §2, and added a real-vs-invented column to §11's stand-in table. **Flagged to human: the same ambiguity exists in `paper_method.md` §8 and `paper_results.md` R5 — neither explicitly states no recorded gesture is a grasp/release action.** Human confirmed: add it. Rewrote `paper_method.md` §8's command-mapping bullet to state plainly that the primitives are a hypothetical vocabulary, not recorded gestures, with the concrete squat→grasp (99.7% recall)/bow→release (98.5%) example; added a one-clause forward-pointer in `paper_results.md` R5's opening sentence. Tex regenerated via `md2tex.py`.

## [2026-07-12] update | LaTeX output switched to IEEE Transactions template (double column, Times)
Human asked for the tex output in IEEE double-column format with Times New Roman, IEEE template. `main.tex` switched from plain `article` to `\documentclass[journal]{IEEEtran}` (double column, standard IEEE Transactions class — matches the THMS venue). Font: `mathptmx` (Times-metric-compatible, pdflatex/Overleaf-native — human chose this over true XeLaTeX+fontspec Times New Roman for zero compile friction). Removed `geometry` (conflicts with IEEEtran's own page geometry). Added proper IEEE front matter: `\author` with a `\thanks` affiliation block (**placeholder — inferred from git config `pt23@fsu.edu` → Florida State University, flagged TODO for human to verify**), a real `\begin{abstract}` environment, and an `\begin{IEEEkeywords}` list (authored, not derivable from markdown). `md2tex.py` gained `extract_abstract_ieee()` — pulls only the `## Full abstract` section's bare paragraph (no draft/revision commentary, no "tight version" variant) into new `abstract_ieee.tex`; `paper_abstract` dropped from the main per-section `FILES` loop since the old `abstract.tex` (full draft dump incl. revision notes) is no longer referenced by `main.tex` and would go stale — deleted. **Wide-table handling** (human chose float-spanning over shrink-only): tables with ≥5 columns now emit as `table*` floats spanning both columns (most result tables — 13 of 20 in `results.tex` — hit this threshold) with an auto-caption from the nearest preceding markdown header (heuristic, not always precise — two tables under the same header get the same caption, e.g. both R6 tables; worth hand-tightening captions in Overleaf); narrower tables stay inline `\small` `center`+`tabular` as before. Regenerated all `.tex` outputs. **Could not compile-verify locally — no TeX engine installed in this environment (confirmed: pdflatex/xelatex/lualatex all absent).** Flagged to human: verify in Overleaf, in particular float placement with 13 table* environments in one file (LaTeX's float queue can push tables far from their reference point in float-heavy two-column docs — a known tuning issue, not caught by this pass) and the placeholder affiliation.

## [2026-07-13] update | DA_GestureRecognition-clean created: full script/data migration for a reproducible rerun
Human requested a clean-slate reproducibility base: a new sibling repo (not a branch — branches don't clear untracked disk bulk, `trained_models/` is 234GB and 99.99% gitignored, so a branch switch would leave the same clutter). Set up `~/projects/DA_GestureRecognition-clean/` as a fresh git repo (`github.com/prabin172/DA_GestureRecognition-clean`, private, personal account for now). Full script inventory done via subagent (cross-referenced `tasks.md`/`RESEARCH_LOG.md`/every wiki results page against ~50 root `temp_*` scripts + `src/scripts/`): ~35 load-bearing, ~35 dead/superseded (DANN family, DSTformer early experiments, cleaned-source slerp cleaning, swing-mode-only runs, unreferenced plot one-offs). Reorganized the load-bearing set into `scripts/{pretrain,data_pipeline,main_experiment,external/{czu,utd},controller,orchestration}/` (was all flat in repo root) — every relocated script's root-path resolution was re-depthed and numerically verified to resolve to the true repo root (caught and fixed a bug in my own first-pass verification regex before trusting the "all clean" result). **Corrections found during migration, not before:** two near-duplicate NTU pretrain scripts existed for supLP120 and mae (writing differently-named, unused checkpoints) — dropped the dead one of each pair; `IMU_batch_processor_v2.py` silently imports shared utilities from "dead" v1 (missed by the inventory agent, caught on inspection, v1 carried over as a dependency); a hardcoded machine-specific absolute path in the CZU multiseed-pooling script fixed to be relative/portable; two scripts imported siblings by bare old filename, fixed to new module paths. Data migrated with a provenance manifest (`migration/MANIFEST.md` + SHA256 checksums for the two irreplaceable items): **`Data_Processed/ntu_quats` (976MB) — discovered the raw `NTU-SkeletalData/` source no longer exists anywhere on this machine, making this processed form the sole surviving copy** (NTU RGB+D isn't casually re-downloadable); `DataCollection/` (6.8GB, raw Xsens mvnx, has the known PII issue). Both copied via rsync and verified byte-identical post-copy (43490/43490 and 64/64 files, sha256sum -c exit 0). `external_data/` (8.3GB, public CZU/UTD downloads) copied, size-verified only. Also closed a real reproducibility gap that had nothing to do with the migration itself: **no `requirements.txt` existed anywhere in the original repo** — frozen now (90 packages, Python 3.12.3, captured before any environment drift). `wiki/`, `paper/`, `tasks.md`, `RESEARCH_LOG.md` copied wholesale; `CODEBASE_DESCRIPTION.md`/`projectAnalysis.md`/`LLM Wiki.md` deliberately left behind (wiki/index.md already calls these superseded-for-navigation); old `SESSION_HANDOFF.md` left behind (describes swing-mode/old-branch state not relevant to a fresh start) — a new one written for the clean repo instead. Old repo's `trained_models/` (234GB) intentionally not touched at all — full rerun starts empty. **Follow-up correction, same session:** [[controller]] wrongly claimed the prototype's numbers "are not cited as evidence" — the inventory agent found `scripts/controller/controller_sim.py`'s exact numbers ARE R5's opening headline sentence; fixed.

## [2026-07-13] update | A2 pooling reproducibility gap closed + controller.md correction re-synced
New `scripts/main_experiment/analyze_a2_multiseed.py` reproduces the pooled A2 table byte-for-byte from raw per-seed data (verified via diff against the existing `a2_pooled_results.csv`) and adds a paired-t stats companion — closes the gap flagged during the `DA_GestureRecognition-clean` migration (A2's pooled numbers previously traced to an uncommitted script). Updated [[a2-subject-scaling]] caveat. Propagated to the clean repo (`scripts/main_experiment/analyze_a2_multiseed.py`, path-depth verified). Also re-synced `wiki/` to the clean repo to pick up the [[controller]] correction from earlier this session (prototype numbers ARE cited in R5, contrary to the page's prior wrong claim).

## [2026-07-13] update | Migration gap: src/data/action_names.json missing, killed rerun 2s into Stage 1; fixed and relaunched
First full-rerun attempt (launched 2026-07-12 23:53 CDT, PID 2024853) died right after Stage 0 finished — `src/data/ntu_loader.py:23` (`UnifiedNTUDataset`, used by 3 of 4 NTU pretrain scripts: `mae`, `supMAE`, `supLP120`) hard-required `src/data/action_names.json`, which the migration's script inventory never flagged since it's a non-`.py` data file. Audited the rest of `src/data/` and `src/models/` for the same class of gap: `imu_to_ntu_action_map.json` and `ntu_relevant_action_ids.json` were also missing but confirmed used only by dead DANN-era scripts (correctly pruned, not a gap); `src/data/imu_loader.py` and `src/models/dstformer_quat_encoder.py` (defines `DSTformerQuatEncoder`/`DSTformerQuatBlock`) also missing but unimported anywhere in either repo — also correctly pruned. Copied `action_names.json` from the original repo (byte-identical, diff-verified). Commented out the completed Stage 0 line in `RUN_FULL_RERUN.sh` (do-not-rerun rule) and relaunched via nohup — new PID 2136371 (parent chain: `systemd --user` → survives logout, same pattern as attempt 1), confirmed past the failure point into Stage 1 epoch 1/50 with only non-fatal deprecation warnings. First failed log kept as `full_rerun.log.stage0_and_failed_attempt1`; new log is `full_rerun.log`.

## [2026-07-13] update | Second migration gap: ntu_relevant_action_ids.json also missing; earlier "dead code" audit was wrong about it, fixed and relaunched
Attempt 2 (PID 2136371, launched 09:10 CDT after the `action_names.json` fix) got past Stage 0 and ran `pretrain_supLP120.py`'s `all120` mode to full completion (epoch 50/50, checkpoint saved), then died moving to its `relevant23` mode: `scripts/pretrain/pretrain_supLP120.py:35,109` (`RELEVANT_JSON_PATH`) hard-required `src/data/ntu_relevant_action_ids.json`. **This file was one of the four I'd already checked in the previous session's audit and marked "correctly pruned, DANN-era only, unused"** — that was wrong. I'd grepped only the old dead DANN scripts for usage, not the surviving pipeline scripts; `pretrain_supLP120.py` (which does survive, is `[1/4]` in `01_pretrain.sh`) references it by literal filename. Lesson for future missing-file triage in this repo: grep the exact filename across all of current `scripts/`+`src/`, not just the scripts known to be dead — a file can be both "used by dead code" and "also used by live code." Copied `ntu_relevant_action_ids.json` from the original repo (byte-identical, diff-verified). Re-swept all literal `.json`/`.npy`/`.npz`/`.pkl`/`.csv`/`.txt` path references across `scripts/`+`src/` afterward as a broader check — everything else matched is a script writing its own output (result CSVs), not a missing input; `imu_to_ntu_action_map.json` and `imu_loader.py` remain correctly-confirmed zero-reference and unrestored. Relaunched via nohup — new PID 2161288 (parent chain confirmed → `systemd --user`, survives logout). `pretrain_supLP120.py` reruns `all120` from scratch (~6 min harmless duplicate work) before reaching the now-fixed `relevant23` path. Attempt 2's log kept as `full_rerun.log.attempt2_died_relevant23_missing`; new log is `full_rerun.log`.

## [2026-07-13] query | Attempt 3 confirmed healthy through Stage 1 and into Stage 2
Checked in on the relaunched rerun (PID 2161288, `systemd --user` detached) after the two migration-gap fixes this session. **Stage 1 (NTU pretraining) completed cleanly at 11:48 CDT** — all 4 objectives (supLP120 incl. `relevant23`, mae, supMAE, supcon), zero errors. **Stage 2 (main Xsens LOSO) started right after** and is mid-loop through subjects/methods (sub9 supMAE at epoch 70s/80, ~99% val accuracy at last check) with no tracebacks. This is well past where attempts 1 (`action_names.json`) and 2 (`ntu_relevant_action_ids.json`) died, confirming both fixes hold. No action needed — process runs unattended.

## [2026-07-14] fix | Attempt 5 died at Stage 5 (OOV): stale v1 path + missing supcon in loso_leave_class_out_fewshot.py; fixed both, attempt 7 running
Checked the rerun: Stage 4 (A2 subject-scaling) completed cleanly at 00:17:37 CDT (full 3-seed sweep + pooled analysis), then Stage 5 (OOV) crashed instantly — `loso_leave_class_out_fewshot.py:58` hardcoded the stale v1 `Data_Processed/imu_quats` as `IMU_DIR` (same class as attempt 4's `mmd_domain_gap.py`; line 41 of the same file ironically finds project root via `imu_quats_v2`). Fixed to `imu_quats_v2`. Swept every Stage 5-9 entry point for the same class — clean (`loso_fulltrain_calibration.py`'s v1 fallback is never exercised; every orchestration call exports `LOSO_IMU_DIR`). Attempt 6 relaunch then hit the second latent Stage-5 bug: `ValueError: Unknown method supcon` — `METHOD_CONFIGS` predates supcon-as-first-class while `05_oov.sh` passes it (same "supcon bolted on later" class as attempt 4's missing `--include-supcon`). Added the `supcon` entry mirroring `loso_fulltrain_calibration.py` (ckpt `trained_models/ContrastiveNTU/supcon_epoch_50.pth`, verified present). `--dry-run` with all 5 methods resolved clean: 550 base + 2200 calibration runs. Attempt 7 relaunched (PID 4147191, stages 0-4 commented out in `RUN_FULL_RERUN.sh`), confirmed past both crash points and into Stage-5 training epochs. Logs kept: `full_rerun.log.attempt5_died_stage5_oov_v1path`, `full_rerun.log.attempt6_died_stage5_supcon_missing`. Updated [[oov-leave-class-out]] + SESSION_HANDOFF.md.
